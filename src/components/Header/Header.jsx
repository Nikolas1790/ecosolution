import { Logo } from "components/Logo/Logo";
import { HeaderContainer } from "./Header.styled";

export const Header = () => {
  return (
    <HeaderContainer >
      <Logo />
      Header
    </HeaderContainer>
  );
};
